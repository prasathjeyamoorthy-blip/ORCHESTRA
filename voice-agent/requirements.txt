# Voice Agent Dependencies

# Core FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Audio processing
numpy>=2.0.0

# Audio decoding (required by pan-rag/api/voice.py to decode webm/ogg/wav → WAV)
av==13.1.0
# sounddevice==0.4.6  # For microphone input (CLI recording only)
# scipy>=1.14.0       # If using audio signal processing

# Sarvam AI SDK for STT/TTS
# Note: sarvamai 0.1.5 requires pydantic-core>=2.18.2
sarvamai==0.1.5

# HTTP clients for service connectivity
httpx==0.26
aiohttp==3.9.1

# Redis for caching (voice performance optimization)
redis==5.0.1
hiredis==2.2.3

# Utilities
pydantic>=2.7
pydantic-settings>=2.1.0