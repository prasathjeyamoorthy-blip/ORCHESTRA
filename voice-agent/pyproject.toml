[project]
name = "voice-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "faster-whisper",
    "edge-tts",
    "pydub",
    "sounddevice",
    "numpy",
    "scipy",
    "requests",
    "rank-bm25",
]
